<!DOCTYPE html>
<html>
 <head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= title %></title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" />
  <link rel="stylesheet" href="style2.css" />
  <style>
    .imgthumb {
      width: 320px;
      height: 320px;
      object-fit: contain; /* 画像全体を正方形内に収める */
      background: azure; /* 背景色（任意） */
      display: block;
      padding: 8px; /* 画像の周りに余白を追加 */
    }
    h1 {
      padding:6px;
      text-align:center;
      color:rgb(61, 160, 171);
      background-color:white;
    }
    p.subtitle {
      text-align: center;
      font-size: 12pt;
      color:royalblue;
    }
  </style>
 </head>
 
 <body>
  <!-- Header -->
  <header class="container">
   <h1><%= title %></h1>
   <p class="subtitle"><%= subtitle %></p>
  </header>
  <!-- スティッキー -->
  <div class="sticky-top text-end me-1 mt-2"><a href="#top">&#x1f53c;Top</a> / <a href="#bottom">&#x1f53d;Bottom</a></div>

  <!-- Main content -->
  <article class="container-fluid">
   <div class="d-flex flex-wrap mt-4 mb-5">
    <% for (const im of images) { %>
     <a href="/showone?path=<%= im %>" target="_blank">
       <img name="img1" src="/image?path=<%= im %>" class="imgthumb">
     </a>
    <% } %>
   </div>
  </article>

  <!-- Footer -->
  <footer class="container">
   <a id="bottom"></a>
   <p class="text-center"><a href="#top">TOP</a></p>
   <p>&nbsp;</p>
   <p>&nbsp;</p>
  </footer>
  </body>
  </html>